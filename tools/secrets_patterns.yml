# Velora secret-detection patterns. Values are regex strings consumed by tools/security_scan.py.
patterns:
  - name: supabase_service_role
    regex: service[_-]?role[\"']?[ \t]*[:=][ \t]*[\"'][A-Za-z0-9_.-]{40,}[\"']
    severity: critical
  - name: supabase_jwt
    regex: eyJ[A-Za-z0-9_-]{100,}
    severity: critical
  - name: stripe_secret
    regex: sk_live_[A-Za-z0-9]{24,}
    severity: critical
  - name: stripe_restricted
    regex: rk_live_[A-Za-z0-9]{24,}
    severity: critical
  - name: sendgrid_key
    regex: SG\.[A-Za-z0-9_-]{16,}\.[A-Za-z0-9_-]{16,}
    severity: critical
  - name: private_key
    regex: -----BEGIN (?:RSA |EC |OPENSSH |DSA )?PRIVATE KEY-----
    severity: critical
  - name: api_key_assignment
    regex: (?i)\b(?:api[_-]?key|client[_-]?secret|secret[_-]?key)[ \t]*[:=][ \t]*[\"'][^\"']{12,}[\"']
    severity: high
  - name: password_literal
    regex: (?i)\bpassword[ \t]*[:=][ \t]*[\"'][^\"']{8,}[\"']
    severity: high
  - name: hardcoded_demo_credential
    regex: (?:Maha@Owner2026|Maha@Admin2026|owner@maha\.com|admin@maha\.com)
    severity: high
